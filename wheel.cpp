#include "wheel.h"
#include <fstream>
//#include "RegularGameWheel.txt"

//wheel is singleton class (one instance only) that is an object made up of 24 pannel objects

// some pannels have values, some have values with a bonus, some are just bad (lose a turn, bankrupt, etc.)

//rounds: toss-up puzzle, (2) regular games, championship game, bonus puzzle


//create wheel by reading in lines from text file and storing 1st part as int, second as string


